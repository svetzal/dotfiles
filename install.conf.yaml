- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.aws/config: aws/config
    ~/.config: config
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
